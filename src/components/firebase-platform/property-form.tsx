import type { Membership } from "@/src/domain/auth/types";
import { createProperty } from "@/app/(platform)/properties/new/actions";
import { PageHeader } from "@/src/components/ui/page-header";

const field="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-base outline-none focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100";
export function FirebasePropertyForm({membership,welcome=false}:{membership:Membership;welcome?:boolean}){
  return <div><PageHeader eyebrow="Property Management" title="Add your first property" description="Your property stays private. Once it is saved, Request Once lets you submit a need and follow it through vendor completion."/>{welcome?<p role="status" className="mt-6 max-w-3xl rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">Organization created. Add the first property to start using Request Once.</p>:null}<form action={createProperty} className="mt-8 grid max-w-3xl gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:grid-cols-2"><input type="hidden" name="organizationId" value={membership.organizationId}/><label className="sm:col-span-2">Property name<input className={field} name="name" required/></label><label className="sm:col-span-2">Street address<input className={field} name="addressLine1" required/></label><label className="sm:col-span-2">Suite or building<input className={field} name="addressLine2"/></label><label>City<input className={field} name="city" required/></label><label>State<input className={field} name="stateCode" minLength={2} maxLength={2} required/></label><label>Postal code<input className={field} name="postalCode" required/></label><div className="sm:col-span-2"><button className="min-h-11 rounded-full bg-slate-950 px-6 text-sm font-semibold text-white">Create property</button></div></form></div>;
}
