import assert from "node:assert/strict";import {readFileSync} from "node:fs";import test from "node:test";
const actions=readFileSync(new URL("../app/(platform)/vendor/opportunities/actions.ts",import.meta.url),"utf8");const feed=readFileSync(new URL("../app/(platform)/vendor/opportunities/page.tsx",import.meta.url),"utf8");const detail=readFileSync(new URL("../app/(platform)/vendor/opportunities/[id]/page.tsx",import.meta.url),"utf8");const migration=readFileSync(new URL("../supabase/migrations/202607200030_vendor_opportunity_responses.sql",import.meta.url),"utf8");
test("assigned vendor feed is scoped to its organization",()=>assert.match(feed,/assigned_vendor_organization_id",vendor\.organizationId/));test("unrelated vendors cannot respond",()=>assert.match(actions,/assigned_vendor_organization_id!==vendor\.organizationId/));test("accept records the vendor and timestamp",()=>{assert.match(actions,/accepted_at:new Date/);assert.match(actions,/accepted_vendor_organization_id:vendor\.organizationId/)});test("decline returns the request for review",()=>{assert.match(actions,/status:"reviewing"/);assert.match(actions,/declined_vendor_organization_id/)});test("details reveal contacts only after acceptance",()=>assert.match(detail,/Exact property and contact details become available after you accept/));test("response migration and policies are present",()=>{assert.match(migration,/accepted_at/);assert.match(migration,/read_assigned_vendor/)});
