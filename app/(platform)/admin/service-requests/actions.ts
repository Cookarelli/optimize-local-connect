"use server";
import { revalidatePath } from "next/cache";import { z } from "zod";import { requireUser } from "@/src/lib/auth/session";import { createSupabaseAdminClient } from "@/src/lib/supabase/admin";
const schema=z.object({requestId:z.string().uuid(),status:z.enum(["reviewing","assigned","completed","cancelled"]),vendorId:z.string().uuid().optional(),note:z.string().trim().max(1000).optional()});
const createSchema=z.object({organizationId:z.string().uuid(),propertyId:z.string().uuid().optional(),propertyName:z.string().trim().min(2).max(160),address:z.string().trim().min(4).max(300),city:z.string().trim().min(2).max(120),categoryId:z.string().uuid(),priority:z.enum(["emergency","today","this_week","flexible"]),description:z.string().trim().min(10).max(5000),contactName:z.string().trim().min(2).max(160),phone:z.string().trim().max(50).optional(),email:z.string().trim().email().optional(),vendorNotes:z.string().trim().max(2000).optional(),internalNotes:z.string().trim().max(3000).optional(),share:z.literal("yes")});
export async function adminCreateServiceRequest(formData:FormData){const user=await requireUser();if(!user.isSuperAdmin)throw new Error("Super Admin access required.");const x=createSchema.parse({organizationId:formData.get("organizationId"),propertyId:formData.get("propertyId")||undefined,propertyName:formData.get("propertyName"),address:formData.get("address"),city:formData.get("city"),categoryId:formData.get("categoryId"),priority:formData.get("priority"),description:formData.get("description"),contactName:formData.get("contactName"),phone:formData.get("phone")||undefined,email:formData.get("email")||undefined,vendorNotes:formData.get("vendorNotes")||undefined,internalNotes:formData.get("internalNotes")||undefined,share:formData.get("share")});const db=createSupabaseAdminClient();const {data,error}=await db.from("property_manager_service_requests").insert({organization_id:x.organizationId,property_id:x.propertyId??null,vendor_category_id:x.categoryId,requested_by:user.id,address:x.address,problem_description:x.description,priority:x.priority,preferred_contact:x.phone?"phone":"email",contact_phone:x.phone??null,contact_email:x.email??null,status:"submitted",submitted_at:new Date().toISOString(),request_source:"admin_entered",property_manager_contact_name:x.contactName,vendor_visible_notes:x.vendorNotes??null,internal_admin_notes:x.internalNotes??null,permission_to_share_confirmed:true,permission_to_share_confirmed_at:new Date().toISOString(),manual_property_name:x.propertyName,manual_city:x.city}).select("id").single();if(error)throw new Error("Unable to create request.");await db.from("property_manager_service_request_history").insert({request_id:data.id,actor_user_id:user.id,status:"submitted",note:"Entered by admin on behalf of property manager",vendor_visible:false});revalidatePath("/admin/service-requests");}
export async function managePropertyManagerServiceRequest(formData:FormData){const user=await requireUser();if(!user.isSuperAdmin)throw new Error("Super Admin access required.");const input=schema.parse({requestId:formData.get("requestId"),status:formData.get("status"),vendorId:formData.get("vendorId")||undefined,note:formData.get("note")||undefined});const db=createSupabaseAdminClient();const {data:request}=await db.from("property_manager_service_requests").select("vendor_category_id").eq("id",input.requestId).single();if(!request)throw new Error("Request not found.");if(input.vendorId){const {data:match}=await db.from("vendor_category_assignments").select("vendor_organization_id").eq("vendor_organization_id",input.vendorId).eq("vendor_category_id",request.vendor_category_id).maybeSingle();if(!match)throw new Error("Vendor must match the request category.");}const patch={status:input.status,assigned_vendor_organization_id:input.vendorId??null,assigned_at:input.status==="assigned"?new Date().toISOString():null,completed_at:input.status==="completed"?new Date().toISOString():null};const {error}=await db.from("property_manager_service_requests").update(patch).eq("id",input.requestId);if(error)throw new Error("Unable to update request.");await db.from("property_manager_service_request_history").insert({request_id:input.requestId,actor_user_id:user.id,status:input.status,note:input.note??null});revalidatePath("/admin/service-requests");}
